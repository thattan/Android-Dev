<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.weddingrsvp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/wedding_rsvp_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/wedding_rsvp_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WeddingRSVP">
        <activity android:name=".SeeAllActivity"></activity>
        <activity android:name=".ViewRsvpActivity" />
        <activity android:name=".RsvpActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>